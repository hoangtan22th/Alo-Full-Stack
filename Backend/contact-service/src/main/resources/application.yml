server:
  port: 8082 # Port riêng cho contact-service

spring:
  application:
    name: contact-service
  datasource:
    url: jdbc:mariadb://localhost:3306/alo_contact_db
    username: root
    password: root
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update # C?c k? l?i h?i: T? ??ng t?o b?ng trong DB d?a trên code Java
    show-sql: true # B?t cái này ?? log console in ra câu l?nh SQL
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MariaDBDialect